(function ($) {
	"use strict";

    jQuery(document).ready(function($){ 
        $('select').niceSelect();  
        $('.mfp-btn').magnificPopup({
            type: 'image' 
          }); 
    });
  
}(jQuery));	